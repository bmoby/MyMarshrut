


<%unless user_signed_in?%>
<nav class="navbar navbar-default navbar-static-top">
	<div class='container-fluid text-center'>
		<div class='navbar-brand'>
			<a href='<%=root_path%>'><i class="fa fa-bus" aria-hidden="true"></i></a>
		</div>
	<ul class='notconnectedlinks nav navbar-nav navbar-right hidden-xs'>
	<li><a href='<%= new_user_session_path%>'><i class="iconsignin fa fa-sign-in"></i>  </br>вход </a></li>
	<li><a href='<%= new_user_registration_path%>'><i class="fa fa-pencil" aria-hidden="true"></i></br>регистрация</a></li>
	</ul>
	<ul class='notconnectedlinksmobile visible-xs'>
	<li><%= link_to new_user_session_path, class:'newusersessionclass' do%>
		<i class="iconsignin fa fa-sign-in"></i> вход
		<%end%>
	</li>
	<li><%= link_to new_user_registration_path, class:'newusersessionclass' do%>
		<i class="fa fa-pencil" aria-hidden="true"></i> регистрация
		<%end%>
	</li>
	</ul>
	</div>
</nav>
<%else%>

<nav class="navbar navbar-default navbar-static-top">
	<div class='container-fluid text-center'>
		<div class='navbar-header'>
			<div class='navbar-brand'>
			<a href='<%=root_path%>'><i class="fa fa-bus" aria-hidden="true"></i></a>
		</div>
				<% if current_user.mailbox.conversations.unread(current_user).count == 0 %>
					<button type'button' class='navbar-toggle' data-toggle='collapse' data-target='.navbar-collapse'>
						<span class='sr-only'>navigateur</span>
						<span class='icon-bar'></span>
						<span class='icon-bar'></span>
						<span class='icon-bar'></span>
					</button>
				<%else%>
					<button type'button' class='notibtn navbar-toggle' data-toggle='collapse' data-target='.navbar-collapse'>
						<div>
						<span class='sr-only'>navigateur</span>
						<p class='notif'><i class="fa fa-bell" aria-hidden="true"></i> <%=current_user.mailbox.conversations.unread(current_user).count%></p>
						</div>
					</button>
				<%end%>
		</div>
		<ul class='notconnectedlinks nav navbar-nav navbar-right collapse navbar-collapse text-center'>
			<% if current_user.mailbox.conversations.unread(current_user).count == 0 %>
			<li><%= link_to conversations_path(current_user) do%>
			<i class="glyphicon glyphicon-envelope"></i> - Мои сообщения
			<%end%>
			</li>
			<%else%>
			<li>
			<%= link_to conversations_path(current_user) do%>
			<p class='notif'><i class="glyphicon glyphicon-envelope"></i> - У вас  <%=current_user.mailbox.conversations.unread(current_user).count%> сообщение</p>
			<%end%>
			</li>
			<%end%>
            <li> <%= link_to  edit_user_registration_path do%>
				<i class="glyphicon glyphicon-cog"></i> - Настройки профиля
				<%end%>
			</li>
			<%if current_user.voditel?%>
            <li> <%= link_to  annonces_moiobyavlenie_path do%>
				<i class="glyphicon glyphicon-tasks"></i> - Мои объявление
				<%end%>
			</li>
			<%else%>
            <li> <%= link_to  new_annonce_path do%>
				<i class="glyphicon glyphicon-tasks"></i> - Создать объявление
				<%end%>
			</li>
			<%end%>
            	<li role="separator" class="divider"></li>
            <li><%= link_to destroy_user_session_path, :method => :delete do%>
				<i class='glyphicon glyphicon-off'></i> - Выход 
				<%end%>
			</li>
		</ul>
	</div>
</nav>
<%end%>