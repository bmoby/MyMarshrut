		<%= render 'shared/navbar' %>
<div class='container-fluid text-center'>
	<div class='row vxodline'>
	<h2><i class='glyphicon glyphicon-link'></i> Вход</h2>
	</div>
</br></br></br>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email, 'Ваш E-mail' %><br />
    <%= f.email_field :email, autofocus: true, class:'inputter' %>
  </div>

  <div class="field">
    <%= f.label :password, 'Ваш пароль' %><br />
    <%= f.password_field :password, autocomplete: "off", class:'inputter' %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me, class: 'check' %>
      <%= f.label :remember_me, 'запомнить?', class: 'check-label' %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Войти", class:'btn btn-info' %>
</br></br>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>