		<%= render 'shared/navbar' %>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <div class='container-fluid text-center'>
	  <% if resource.errors.any? %>
	    <div id="error_explanation">
	      <h2>Пожалуйста, заполните следующие поля и нажмите на «Подтвердить» </h2>

		  <ul class='listerreurs'>
		      <% resource.errors.each do |attribute, message| -%>
		          <% if message.is_a?(String)%>
		              <li><p><%= message %></p></li>
		          <% end %>
		      <% end -%>
		  </ul>
	    </div>
	  <% end %>
<div class=' zapis text-center'>
	<div class='row'>
	<div class=' registraciya-title text-center'>
		<h2>Регистрация</h2>
	</div>
	</div>
</br></br>
  <div class="field">
	<%= f.text_field :prenom, autocomplete: "off", class: 'inputter obligatoir', :placeholder => 'Придумайте себе имя'%>
  </div>
  
  <div class="field">
    <%= f.email_field :email, autocomplete: "off", class: 'inputter obligatoir', :placeholder => 'Ваш E-mail' %>
  </div>
  
  <div class="field">
    <%= f.telephone_field :mobile_number, autocomplete: "off", value: nil, class: 'inputter', :placeholder => 'Ваш телефон' %>
  </div>

  <div class="field parol">
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> символов минимум)</em>
    <% end %><br/>
    <%= f.password_field :password, autocomplete: "off", value: nil, class:'inputter obligatoir', :placeholder => 'Пароль' %>
  </div>

  <div class="field parol">
    <%= f.password_field :password_confirmation, value: nil, autocomplete: "off", class:'inputter obligatoir', :placeholder => 'Повторить пароль' %>
  </div>
	
  <div class="actions sub">
	  <p>Нажав на кнопку «Подтвердить», вы соглашаетесь с Правилами MyMarshrut.</p></br>
	  
    <%= f.submit "Подтвердить", class: "btn btn-info" %>
  </div>
<% end %>
</br></br></br>
</div>

</div>
</div>
